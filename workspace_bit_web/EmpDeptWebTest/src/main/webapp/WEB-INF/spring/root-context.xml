<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->
		
		<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		    <!-- JDBC Driver 클래스 명 설정 -->
		    <property name="driverClassName" value="${driver}" />
		    <!-- connection url -->
		    <property name="url" value="${url}" />
		    <!-- DB 사용자 계정 설정 -->
		    <property name="username" value="${username}" />
		    <!-- DB 사용자 패스워드 설정 -->
		    <property name="password" value="${password}r" />
		</bean>
		<context:annotation-config/>
		<bean class="org.mybatis.spring.SqlSessionFactoryBean" id="sqlSessionFactoryBean">
			<property name="dataSource" ref="dataSource"/>
			<property name="mapperLocations" value="com/bit/empapp/dao/**/Mapper.xml"/>
			<property name="typeAliasesPackage" value="com.bit.empapp.vo"/><!-- typeAllases,typeAllasesPackage -->
		</bean>
		
		<bean class="org.mybatis.spring.SqlSessionTemplate" id="sqlSession">
			<constructor-arg index="0" ref="sqlSessionFactoryBean"/>
		</bean>
		<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer"> <!-- autoWired -->
			<property name="basePackage" value=""/>
		</bean>
		
</beans>
